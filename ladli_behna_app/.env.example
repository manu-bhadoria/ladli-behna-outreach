# ElevenLabs Conversational AI agent for CM Dr. Mohan Yadav voice bot.
# Run `python setup_agent.py` after filling in XI_API_KEY and (optionally) the
# pre-cloned voice id to auto-create the agent and populate VOICE_AGENT_ID below.

VOICE_AGENT_ID=""
VOICE_API_KEY=""

# Optional basic-auth gate for the entire site (leave blank to disable).
BASIC_AUTH_PASSWORD=""
